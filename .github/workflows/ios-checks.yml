name: Reusable iOS checks

on:
  workflow_call:

permissions:
  contents: read

jobs:
  swift_package:
    name: Swift package + coverage
    runs-on: ubuntu-latest
    timeout-minutes: 15
    container:
      image: swift:6.2
    steps:
      - uses: actions/checkout@v7
      - name: Run tests and coverage gate
        run: ios/TableSlateIOS/Scripts/check_coverage.sh
      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: swift-coverage
          retention-days: 14
          if-no-files-found: warn
          path: ios/TableSlateIOS/coverage

  ios_ui_e2e:
    name: XCUITest · ${{ matrix.platform }}
    runs-on: macos-26
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: iPhone
            device_name: iPhone 17 Pro
            artifact_dir: e2e-artifacts/ios-iphone
          - platform: iPad
            device_name: iPad Pro 13-inch (M5)
            artifact_dir: e2e-artifacts/ios-ipad
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'
          cache: pip
      - name: Install tools
        run: |
          python -m pip install --disable-pip-version-check -e .
          brew install xcodegen
      - name: Run UI flows
        run: >-
          python -m invoke ios-ui-e2e
          --device-name="${{ matrix.device_name }}"
          --artifact-dir="${{ matrix.artifact_dir }}"
      - name: Upload UI evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: ios-ui-e2e-${{ matrix.platform }}
          retention-days: 14
          if-no-files-found: warn
          path: ${{ matrix.artifact_dir }}
